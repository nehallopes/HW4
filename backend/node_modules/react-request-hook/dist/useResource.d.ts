import { Canceler } from 'axios';
import { Payload, RequestError, Request, RequestDispatcher, Arguments } from './request';
declare type RequestState<TRequest extends Request> = {
    data?: Payload<TRequest>;
    error?: RequestError;
    isLoading: boolean;
};
export declare type UseResourceResult<TRequest extends Request> = [RequestState<TRequest> & {
    cancel: Canceler;
}, RequestDispatcher<TRequest>];
export declare function useResource<TRequest extends Request>(fn: TRequest, defaultParams?: Arguments<TRequest>): UseResourceResult<TRequest>;
export {};
